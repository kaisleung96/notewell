# Maintenance

Keep the vault healthy:

- Fix broken wikilinks.
- Add missing frontmatter.
- Connect orphan pages when a meaningful relationship exists.
- Rebuild `.notewell/` after structural changes.
- Run `notewell lint` before completion and resolve errors before handing off.
- MCP is optional and embeddings are optional during maintenance; the JSON index
  is the required baseline cache.
