# Query Workflow

1. Search the wiki before answering.
2. Cite relevant wiki pages and raw sources.
3. If the answer contains durable knowledge, write back to `wiki/questions/`,
   `wiki/analyses/`, or `wiki/playbooks/` as appropriate.
4. Rebuild the JSON index after write back.
5. Run `notewell lint` before completion.
